{
  "name": "lms-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "echo 'Use per-app dev scripts'",
    "build": "echo 'Use per-app build scripts'",
    "lint": "echo 'Use per-app lint scripts'"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  }
}
